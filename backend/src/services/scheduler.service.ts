import { log } from "../utils/logger";

/**
 * Scheduler service - not used in minimal implementation
 */
export const schedulerService = {
  async runScheduler() {
    log("Scheduler service disabled in minimal implementation");
  }
};

